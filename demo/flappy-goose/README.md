# Flappy Goose

A small HTML5-canvas browser game that serves as a demo client for the
from-scratch Redis server in the repo root — it submits scores to a leaderboard
backend and renders a top-scores list. It currently runs against a stock Redis
instance on an Azure VM, and will be pointed at this project's own server once
its command handling lands.
